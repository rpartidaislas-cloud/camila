(function(){
'use strict';
var state=null,ready=false,pointers=new Map(),lastDistance=0,lastCenter=null;
function el(id){return document.getElementById(id);}
function cover(st){var a=(st.quarter*90+st.angle)*Math.PI/180,c=Math.abs(Math.cos(a)),s=Math.abs(Math.sin(a));return Math.max((st.canvas.width*c+st.canvas.height*s)/st.img.naturalWidth,(st.canvas.width*s+st.canvas.height*c)/st.img.naturalHeight);}
function render(grid){if(!state)return;var st=state,c=st.canvas,ctx=c.getContext('2d'),a=(st.quarter*90+st.angle)*Math.PI/180;st.cover=cover(st);var sc=st.cover*st.zoom,bw=(Math.abs(st.img.naturalWidth*Math.cos(a))+Math.abs(st.img.naturalHeight*Math.sin(a)))*sc,bh=(Math.abs(st.img.naturalWidth*Math.sin(a))+Math.abs(st.img.naturalHeight*Math.cos(a)))*sc,mx=Math.max(0,(bw-c.width)/2),my=Math.max(0,(bh-c.height)/2);st.x=Math.max(-mx,Math.min(mx,st.x));st.y=Math.max(-my,Math.min(my,st.y));ctx.fillStyle='#000';ctx.fillRect(0,0,c.width,c.height);ctx.save();ctx.translate(c.width/2+st.x,c.height/2+st.y);ctx.rotate(a);ctx.scale(sc,sc);ctx.drawImage(st.img,-st.img.naturalWidth/2,-st.img.naturalHeight/2);ctx.restore();if(grid!==false){ctx.strokeStyle='rgba(255,255,255,.52)';ctx.lineWidth=1;[1/3,2/3].forEach(function(p){ctx.beginPath();ctx.moveTo(c.width*p,0);ctx.lineTo(c.width*p,c.height);ctx.stroke();ctx.beginPath();ctx.moveTo(0,c.height*p);ctx.lineTo(c.width,c.height*p);ctx.stroke();});ctx.strokeStyle='#fff';ctx.lineWidth=2;ctx.strokeRect(1,1,c.width-2,c.height-2);}}
function reset(){if(!state)return;state.quarter=0;state.angle=0;state.zoom=1;state.x=0;state.y=0;el('photo-adjust-range').value=0;el('photo-adjust-angle').textContent='0°';render();}
function exportPhoto(){var st=state,aspect=st.canvas.width/st.canvas.height,ow=aspect>=1?1280:Math.round(1280*aspect),oh=aspect>=1?Math.round(1280/aspect):1280,f=ow/st.canvas.width,out=document.createElement('canvas'),ctx=out.getContext('2d'),a=(st.quarter*90+st.angle)*Math.PI/180,sc=st.cover*st.zoom*f;out.width=ow;out.height=oh;ctx.fillStyle='#000';ctx.fillRect(0,0,ow,oh);ctx.save();ctx.translate(ow/2+st.x*f,oh/2+st.y*f);ctx.rotate(a);ctx.scale(sc,sc);ctx.drawImage(st.img,-st.img.naturalWidth/2,-st.img.naturalHeight/2);ctx.restore();return out.toDataURL('image/jpeg',.92);}
function close(value){var st=state;if(!st)return;el('photo-adjust').classList.remove('open');state=null;pointers.clear();st.resolve(value);}
function center(){var a=Array.from(pointers.values());return{x:(a[0].x+a[1].x)/2,y:(a[0].y+a[1].y)/2};}
function distance(){var a=Array.from(pointers.values());return Math.hypot(a[0].x-a[1].x,a[0].y-a[1].y);}
function init(){if(ready)return;ready=true;var canvas=el('photo-adjust-canvas');canvas.addEventListener('pointerdown',function(e){if(!state)return;pointers.set(e.pointerId,{x:e.clientX,y:e.clientY});if(canvas.setPointerCapture)canvas.setPointerCapture(e.pointerId);if(pointers.size===2){lastDistance=distance();lastCenter=center();}e.preventDefault();});canvas.addEventListener('pointermove',function(e){if(!state||!pointers.has(e.pointerId))return;var prev=pointers.get(e.pointerId);pointers.set(e.pointerId,{x:e.clientX,y:e.clientY});if(pointers.size===1){state.x+=e.clientX-prev.x;state.y+=e.clientY-prev.y;}else if(pointers.size===2){var d=distance(),c=center();if(lastDistance)state.zoom=Math.max(1,Math.min(5,state.zoom*d/lastDistance));if(lastCenter){state.x+=c.x-lastCenter.x;state.y+=c.y-lastCenter.y;}lastDistance=d;lastCenter=c;}render();e.preventDefault();});function end(e){pointers.delete(e.pointerId);lastDistance=0;lastCenter=null;}canvas.addEventListener('pointerup',end);canvas.addEventListener('pointercancel',end);el('photo-adjust-range').addEventListener('input',function(){if(!state)return;state.angle=Number(this.value)||0;el('photo-adjust-angle').textContent=state.angle.toFixed(1).replace('.0','')+'°';render();});el('photo-adjust-rotate').addEventListener('click',function(){if(!state)return;state.quarter=(state.quarter+1)%4;state.x=0;state.y=0;state.zoom=1;render();});el('photo-adjust-reset').addEventListener('click',reset);el('photo-adjust-cancel').addEventListener('click',function(){close(null);});el('photo-adjust-use').addEventListener('click',function(){close(exportPhoto());});}
window.ajustarFotoAntesDeGuardar=function(dataUrl,view){return new Promise(function(resolve,reject){var img=new Image();img.onload=function(){init();var modal=el('photo-adjust'),stage=el('photo-adjust-stage'),canvas=el('photo-adjust-canvas');el('photo-adjust-title').textContent='Ajustar '+((view&&view.label)||'fotografía').split('—')[0].trim().toLowerCase();modal.classList.add('open');requestAnimationFrame(function(){var mw=Math.max(240,stage.clientWidth-36),mh=Math.max(220,stage.clientHeight-36),aspect=(view&&view.orientation==='landscape')?4/3:4/5,w=mw,h=w/aspect;if(h>mh){h=mh;w=h*aspect;}canvas.width=Math.max(1,Math.round(w));canvas.height=Math.max(1,Math.round(h));state={img:img,canvas:canvas,view:view,quarter:0,angle:0,zoom:1,x:0,y:0,cover:1,resolve:resolve};reset();});};img.onerror=reject;img.src=dataUrl;});};
})();
